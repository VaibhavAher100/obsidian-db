# Changelog

All notable changes to ObsidianDB are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Planned for v0.1.0
- Folder to live table view from YAML frontmatter
- Sort and filter columns
- Inline cell editing (writes back to frontmatter)
- Formula columns: SUM, COUNT, AVG, IF over frontmatter values
- CSV import - converts rows to .md files with frontmatter mapping
- Wikilinks in cells render as clickable links
- Theme-native styling via Obsidian CSS variables
- Optional Dataview integration with graceful fallback
